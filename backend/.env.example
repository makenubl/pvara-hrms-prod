MONGODB_URI=mongodb://localhost:27017/pvara-hrms
JWT_SECRET=your-secret-key-change-this-in-production
PORT=5000
NODE_ENV=development

# Twilio WhatsApp Configuration (Twilio Console)
# NOTE: Do NOT commit real credentials here.
# The backend supports BOTH naming conventions:
# - TWILIO_ACCOUNT_SID / TWILIO_AUTH_TOKEN (preferred)
# - TWILIO_SID / TWILIO_AUTH (legacy)

# Preferred
TWILIO_ACCOUNT_SID=your-twilio-account-sid
TWILIO_AUTH_TOKEN=your-twilio-auth-token

# Legacy (optional)
# TWILIO_SID=your-twilio-account-sid
# TWILIO_AUTH=your-twilio-auth-token

# For sandbox use: whatsapp:+14155238886
# For production use: whatsapp:+14583092310 (example format)
TWILIO_WHATSAPP_NUMBER=whatsapp:+14155238886

# Webhook security token (optional but recommended)
WHATSAPP_WEBHOOK_TOKEN=your-webhook-verification-token

# OpenAI API Key (required if you want the AI agent to use OpenAI parsing + voice transcription)
OPENAI_API_KEY=your-openai-api-key